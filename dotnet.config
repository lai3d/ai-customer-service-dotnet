# .NET 10's `dotnet test` needs to be told which runner a solution uses. xunit v3 runs on
# Microsoft.Testing.Platform, so this is what makes `dotnet test` at the root work.
[dotnet.test.runner]
name = "Microsoft.Testing.Platform"
